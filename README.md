# Site A'Bonbonne

## Preview local

```bash
make preview
```

Se quiser usar outra porta:

```bash
make preview PORT=8080
```

Depois, abra no navegador:

- `http://localhost:4173/index.html`
- ou a porta que você definiu no comando.
